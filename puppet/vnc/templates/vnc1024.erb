service vnc1024
{
	disable = no
	socket_type = stream
	protocol = tcp
	wait = no
	user = nobody
	server = /usr/bin/Xvnc
	server_args = -inetd -query <%= fqdn %> -geometry 1024x768 -depth 16 -once -fp unix/:7100 -securitytypes=none
}
